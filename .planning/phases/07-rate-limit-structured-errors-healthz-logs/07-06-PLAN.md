---
phase: 07
plan: 06
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - tests/test_app.py
  - tests/test_rate_limit.py
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - RATE-06
  - OBS-01
  - OBS-03
  - OBS-04
must_haves:
  truths:
    - "GET /healthz returns 200 + {\"status\":\"ok\"} AND dispatches zero httpx requests upstream."
    - "A 429 short-circuited at the ASGI rate-limit middleware emits exactly one structured log line whose keys are a subset of config.LOG_FIELDS + structlog meta, with tool/database/table = None and error_code = 'rate_limited'."
    - "The 429 synthetic log line contains zero substrings from a hostile-input corpus (canary tokens, FTS5 operators, malformed bytes) — the middleware never parses the request body."
    - "README operator section explicitly states `uvicorn ... --workers 1` AND explains why (in-memory limiter would silently multiply across workers)."
    - "REQUIREMENTS.md traceability marks OBS-02 deferred to v2 per D7-05; the row links to D7-04 / D7-05 rationale."
  artifacts:
    - path: tests/test_app.py
      provides: "Belt-and-suspenders no-upstream test for /healthz using httpx_mock"
      contains: "test_healthz_dispatches_no_httpx_request"
    - path: tests/test_rate_limit.py
      provides: "GREEN tests test_429_log_line_shape + test_logs_no_user_input"
      contains: "test_429_log_line_shape"
    - path: README.md
      provides: "Operator section: single-worker mandate, daily-reset UTC midnight, /internal/upstream-status v2 deferral"
      contains: "--workers 1"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability table marks OBS-02 deferred to v2 with D7-05 reference"
      contains: "OBS-02"
  key_links:
    - from: src/mcp_zeeker/core/middleware/rate_limit.py
      to: src/mcp_zeeker/config.py
      via: "Synthetic 429 log line emits only LOG_FIELDS keys (request_id, tool, database, table, duration_ms, status, ip_prefix, error_code); tests assert no extras"
      pattern: "LOG_FIELDS"
---

<objective>
Lock the /healthz contract (OBS-01) with a belt-and-suspenders no-upstream-call test, GREEN the two OBS-03/04 structured-log tests for the 429 short-circuit path, document the single-worker constraint (RATE-06) and the UTC-midnight reset in the README operator section, and explicitly defer OBS-02 to v2 in REQUIREMENTS.md per D7-05.

Purpose: After this plan, an MCP client OBSERVES a stable /healthz (no upstream leakage) AND an operator OBSERVES three documented contracts in canonical sources — single-worker mandate, daily UTC reset, and OBS-02 deferral. The 429 emitted by 07-01's middleware is now proven to leak no user input into structured logs, closing the INJ-05 chain at the rate-limit layer.
Output: One new test in `test_app.py`; two GREEN tests un-skipped in `test_rate_limit.py`; README operator section additions; REQUIREMENTS.md traceability update.
</objective>

## Phase Goal

**As an** operator running mcp.zeeker.sg in production, **I want to** see /healthz return process-liveness without ever touching upstream, AND read in README that I must run with `--workers 1` because the in-memory rate-limit bucket is per-process, AND see in REQUIREMENTS.md that /internal/upstream-status is honestly deferred to v2, **so that** my deployment configuration and traceability are unambiguous before submission to anthropics/claude-for-legal.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md
@.planning/REQUIREMENTS.md
@src/mcp_zeeker/app.py
@src/mcp_zeeker/config.py
@src/mcp_zeeker/core/middleware/rate_limit.py
@src/mcp_zeeker/core/middleware/access_log.py
@tests/test_app.py
@tests/test_logging.py
@tests/test_rate_limit.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-upstream-call belt-and-suspenders test for /healthz</name>
  <files>tests/test_app.py</files>
  <read_first>
    - tests/test_app.py lines 22–30 (existing `test_healthz_returns_ok_without_upstream` — keep as is; this task ADDS a sibling that uses httpx_mock to make the no-upstream property mechanical)
    - src/mcp_zeeker/app.py lines 92–94 (healthz handler — confirms it does not consult upstream)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § /healthz Contract Lock (belt-and-suspenders test recommendation)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md (note: the existing `test_healthz_returns_ok_without_upstream` is already ✅; this plan ADDS a stricter sibling to lock OBS-01 mechanically)
  </read_first>
  <action>
    In tests/test_app.py, add a new async test `test_healthz_dispatches_no_httpx_request` placed immediately after the existing `test_healthz_returns_ok_without_upstream`. Docstring: "OBS-01 belt-and-suspenders: /healthz MUST NOT dispatch any upstream call. pytest-httpx records every outgoing httpx call; if /healthz emits one, this test fails." Body: function takes both `asgi_client` and `httpx_mock: pytest_httpx.HTTPXMock` fixtures. Do not register any responses on httpx_mock (so an unexpected upstream call would surface as an unmatched request). Issue `resp = await asgi_client.get("/healthz")`. Assert `resp.status_code == 200` and `resp.json() == {"status": "ok"}`. Assert `httpx_mock.get_requests() == []` — zero upstream requests dispatched. Add `import pytest_httpx` at the top of the file if not already present. Do NOT modify or rename `test_healthz_returns_ok_without_upstream` — the existing test is the VALIDATION.md-named gate; the new test is a sibling property assertion.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_app.py::test_healthz_dispatches_no_httpx_request -x` exits 0.
    - `uv run pytest tests/test_app.py::test_healthz_returns_ok_without_upstream -x` exits 0 (existing test preserved).
    - `uv run pytest tests/test_app.py -x` exits 0.
    - `grep -c 'def test_healthz' tests/test_app.py` is `>= 2`.
    - `git diff tests/conftest.py` is empty (single-plan-touch rule preserved).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_app.py::test_healthz_dispatches_no_httpx_request tests/test_app.py::test_healthz_returns_ok_without_upstream -x</automated>
  </verify>
  <done>OBS-01 contract has a mechanical no-upstream-call assertion via pytest-httpx; existing healthz test preserved.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN the 429 log-line-shape + no-user-input tests</name>
  <files>tests/test_rate_limit.py</files>
  <read_first>
    - tests/test_logging.py lines 24–49 (existing `capture_logs` + `allowed_keys` pattern — copy verbatim for the 429 log-shape assertion)
    - src/mcp_zeeker/core/middleware/rate_limit.py (full file — confirm the synthetic 429 log line emits exactly `tool=None, database=None, table=None, duration_ms=..., status="rejected", error_code="rate_limited"` and relies on contextvar merge for request_id/ip_prefix)
    - src/mcp_zeeker/config.py lines 458–467 (LOG_FIELDS locked tuple)
    - tests/_corpus/hostile_inputs.py (Phase 6 hostile-input corpus — for `test_logs_no_user_input` use a small subset: canary tokens, FTS5 operators, `</system>` token; full 5-canary fan-out is Phase 6's domain)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Structured Logging for 429 for the exact field map
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § shared § structlog `capture_logs` Test Pattern
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Per-Task Verification Map (test names this plan GREENs)
  </read_first>
  <action>
    Un-skip and fill the bodies of `test_429_log_line_shape` and `test_logs_no_user_input` in tests/test_rate_limit.py. (1) `test_429_log_line_shape`: imports needed include `import structlog; from structlog.testing import capture_logs; from mcp_zeeker.core.logging import bind_request, clear_request; from mcp_zeeker import config`. Body: `bind_request(request_id="rid-log", ip_prefix="203.0.113")` inside try/finally. Inside the try block: `with capture_logs(processors=[structlog.contextvars.merge_contextvars]) as cap:` drive 21 requests against `rate_limiter` via the captured-send pattern (re-use the helper from 07-01 task 3 — copy the scope/send setup verbatim). After exiting `capture_logs`, find the log entry whose `event == "tool_call"` AND `error_code == "rate_limited"` (one expected). Assert `line["tool"] is None and line["database"] is None and line["table"] is None`. Assert `line["status"] == "rejected"`. Assert `line["request_id"] == "rid-log"` and `line["ip_prefix"] == "203.0.113"` (merged from contextvar). Assert key-set bound: `allowed_keys = set(config.LOG_FIELDS) | {"event", "log_level", "level", "timestamp"}; extra = set(line.keys()) - allowed_keys; assert extra == set()`. The `finally:` calls `clear_request()`. (2) `test_logs_no_user_input`: parametrize over a small hostile-input list directly inline (no need to import the full Phase 6 corpus — pull in three representative strings: `"DROP TABLE users; --"`, `"</system><admin>"`, `'" OR 1=1 --'`). For each hostile string, build a scope with `headers=[(b"x-forwarded-for", hostile.encode("latin-1", errors="replace"))]` AND `headers` also include `(b"content-type", b"application/json")` AND the request body is the same hostile string. Drive 21 requests with the captured-send pattern (each request creates a unique bucket because XFF varies, so the 21st request is from one of the spoofed IPs — to actually hit the rate limit, repeat the hostile XFF value 21 times instead of varying it). After capturing logs, find the synthetic 429 log line; for each hostile string, assert `hostile not in str(line)` — the log line repr contains NO substring of the hostile input. Document in the test docstring that this is the OBS-04 contract: the rate-limit middleware never parses the request body and only the IP-prefix (truncated to /24) appears in `ip_prefix`, so hostile content cannot leak.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rate_limit.py::test_429_log_line_shape -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_logs_no_user_input -x` exits 0.
    - `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` decreases by exactly 2 relative to the post-07-03 state.
    - `uv run pytest tests/test_rate_limit.py -x` exits 0 (no regression).
    - `uv run pytest -x` exits 0 (full suite GREEN).
    - `git diff tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rate_limit.py::test_429_log_line_shape tests/test_rate_limit.py::test_logs_no_user_input -x</automated>
  </verify>
  <done>OBS-03 log-shape and OBS-04 no-input-echo locked by tests; 429 synthetic log line strictly uses the LOG_FIELDS set; hostile-input strings never appear in the log line.</done>
</task>

<task type="auto">
  <name>Task 3: README operator section + REQUIREMENTS.md OBS-02 deferral</name>
  <files>README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md (full file — identify the existing "Operator" or "Deployment" section; if absent, add one at the end after existing content)
    - .planning/REQUIREMENTS.md lines 105–112 (OBS-01..05 entries) AND lines 250–260 (traceability table — OBS-02 row)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md § Decisions D7-04 (defer /internal/upstream-status to v2) and D7-05 (OBS-02 marked deferred in REQUIREMENTS.md)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Manual-Only Verifications (RATE-06 README check and OBS-02 deferred check)
  </read_first>
  <action>
    In README.md, locate the "Operator" or "Deployment" section. If it exists, append a new subsection titled "Single-worker requirement (RATE-06)". If no such section exists, add a top-level `## Operator Notes` section near the end. The subsection MUST contain three paragraphs: (a) "Run with exactly one Uvicorn worker: `uvicorn mcp_zeeker.app:app --host 0.0.0.0 --port 8000 --workers 1`. The in-memory rate-limit bucket is per-process; running with `--workers 2` would silently multiply the effective rate limit by 2 because each worker keeps its own bucket store — a class of bug that only shows up under load. RATE-06 in REQUIREMENTS.md mandates `--workers 1` for v1." (b) "Daily rate-limit counter resets at 00:00 UTC. Anonymous-tier clients near their daily ceiling will see a correlated burst at UTC midnight; the burst (20) + sustained (60/min) windows still apply, so this does not produce a thundering herd." (c) "Upstream health is checked by calling `curl https://data.zeeker.sg/-/metadata.json` from outside the container OR `docker exec <container> curl ...` from inside. The in-process `/internal/upstream-status` endpoint is deferred to v2 (see D7-04). The public `/healthz` endpoint is liveness-only and never consults upstream (OBS-01)." In .planning/REQUIREMENTS.md, locate the traceability table row for OBS-02 (line ~254). Change its status from `Pending` to `Deferred to v2 (D7-05)`. Add a footnote or short addendum to the Deferred Items section near the top of REQUIREMENTS.md or to the section that lists v2 items: "`/internal/upstream-status` (OBS-02) — deferred to v2 per Phase 7 decision D7-04/D7-05. Operators inspect upstream health via `curl https://data.zeeker.sg/-/metadata.json` from outside the container in v1." Do NOT remove OBS-02 from the v1 Observability list — leave the requirement bullet in place at lines ~108–109 but the traceability table is the source of truth for status, and the table now says Deferred. This preserves "honest traceability over false-positive closure" per D7-05.
  </action>
  <acceptance_criteria>
    - `grep -c -- '--workers 1' README.md` is `>= 1`.
    - `grep -c '00:00 UTC' README.md` is `>= 1`.
    - `grep -c 'upstream-status' README.md` is `>= 1`.
    - `grep -c 'OBS-02' .planning/REQUIREMENTS.md` is `>= 2` (original requirement row + traceability + possibly deferred-items section).
    - `grep -c 'Deferred to v2' .planning/REQUIREMENTS.md` is `>= 1`.
    - `grep -n 'OBS-02' .planning/REQUIREMENTS.md | grep -i 'defer'` returns a non-empty line.
    - `uv run pytest -x` exits 0 (no test depends on README content, but documentation changes should not break anything).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -x</automated>
  </verify>
  <done>README documents the three operator-facing facts (single worker, UTC midnight reset, upstream-status v2 deferral); REQUIREMENTS.md traceability honestly marks OBS-02 deferred per D7-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public `/healthz` → operator | Liveness only; must not leak upstream status (OBS-01) |
| Synthetic 429 log line → operator log pipeline | Must contain zero substrings of user input (OBS-04 / INJ-05 carry-forward) |
| Multi-worker deployment → in-memory limiter | Silently multiplies rate limit (RATE-06 documents single-worker mandate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Information Disclosure | 429 synthetic log line contents | mitigate | `test_logs_no_user_input` proves three hostile-input strings never appear in the log line repr; the rate-limit middleware never parses the request body and `ip_prefix` is /24-truncated upstream by `RequestIdMiddleware`; INJ-05 carry-forward from Phase 3 holds at this layer. |
</threat_model>

<verification>
- `uv run pytest tests/test_app.py tests/test_rate_limit.py -x` is GREEN.
- `uv run pytest -x` is GREEN.
- `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` is `0` (all 15 stubs from 07-01 are now GREEN across 07-01/02/03/06).
- README operator section contains `--workers 1`, `00:00 UTC`, and `upstream-status` references.
- REQUIREMENTS.md traceability table marks OBS-02 deferred with D7-05 reference.
</verification>

<success_criteria>
- /healthz no-upstream-call property is mechanically enforced by `test_healthz_dispatches_no_httpx_request`.
- 429 synthetic log line shape and no-user-input invariant are proven in two GREEN tests.
- All 15 originally-stubbed tests in `tests/test_rate_limit.py` are GREEN by the end of Phase 7.
- README operator section explicitly documents single-worker mandate, UTC midnight reset, and upstream-status v2 deferral.
- REQUIREMENTS.md traceability honestly marks OBS-02 deferred to v2 per D7-05.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-06-SUMMARY.md` documenting: the new healthz test name and the pytest-httpx no-request assertion, the two un-skipped log-shape tests, the exact README subsection title chosen, and the REQUIREMENTS.md row + section before/after for the OBS-02 deferral.
</output>
