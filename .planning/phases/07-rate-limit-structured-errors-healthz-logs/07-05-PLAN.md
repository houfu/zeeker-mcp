---
phase: 07
plan: 05
type: execute
wave: 3
depends_on:
  - "07-04"
files_modified:
  - src/mcp_zeeker/core/datasette_client.py
  - tests/test_datasette_client_retry.py
autonomous: true
requirements:
  - ERR-04
must_haves:
  truths:
    - "Two consecutive upstream 502s from `_request_with_retry` raise UpstreamCallFailed with a 'retry exhausted' marker (no third attempt)."
    - "Two consecutive upstream 503s raise UpstreamCallFailed with the same exhaustion marker."
    - "Upstream HTTP 504 raises UpstreamCallFailed immediately (no retry) — pre-existing behavior, re-asserted under the 07-VALIDATION test name."
    - "An httpx.TimeoutException during `_request_with_retry` raises the new QueryTimeoutError subclass so tool handlers can map it to the `query_timeout` catalog code."
  artifacts:
    - path: src/mcp_zeeker/core/datasette_client.py
      provides: "QueryTimeoutError subclass + httpx.TimeoutException catch path"
      contains: "class QueryTimeoutError"
    - path: tests/test_datasette_client_retry.py
      provides: "test_502_twice_raises, test_503_twice_raises, test_504_raises_immediately (renamed to match VALIDATION.md), test_timeout_raises_query_timeout_error"
      contains: "test_502_twice_raises"
  key_links:
    - from: src/mcp_zeeker/core/datasette_client.py
      to: src/mcp_zeeker/core/errors.py
      via: "QueryTimeoutError is a subclass of UpstreamCallFailed; tool handlers translate it to ToolError('query_timeout: ...') using raise_query_timeout from core/errors.py"
      pattern: "class QueryTimeoutError"
---

<objective>
Verify and extend the existing `_request_with_retry` 502/503/504 contract: prove that retry exhaustion on 502/503 raises `UpstreamCallFailed`, that 504 surfaces immediately, and add a new `QueryTimeoutError(UpstreamCallFailed)` subclass + `httpx.TimeoutException` catch path so the locked `query_timeout` catalog code from 07-04 has a real raise site in the upstream call layer.

Purpose: After this plan, an MCP client OBSERVES the full ERR-04 retry contract — exactly one retry with 250ms + uniform(0,250ms) jitter on transient upstream 5xx; immediate raise on 504; and timeouts distinguished from generic upstream-unavailability via a typed subclass. This closes ERR-04 and adds the only Phase 7-owned `query_timeout` raise site (per RESEARCH.md Open Question #3 recommendation; `invalid_query` raise sites remain Phase 4 scope per RESEARCH.md Open Question #2).
Output: New `QueryTimeoutError` class + `except httpx.TimeoutException` branch in `_request_with_retry`; four GREEN tests in `tests/test_datasette_client_retry.py` (two new — exhaustion cases; two existing-but-verifying — rename/confirm names match VALIDATION.md).
</objective>

## Phase Goal

**As an** MCP client of mcp.zeeker.sg, **I want to** know that the server will retry transient upstream 502/503 once with bounded jitter and then surface a stable `upstream_unavailable`, AND that upstream timeouts produce a distinct `query_timeout` code, **so that** my retry strategy can distinguish "they tried and gave up" from "the query was too expensive".

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-04-SUMMARY.md
@src/mcp_zeeker/core/datasette_client.py
@src/mcp_zeeker/core/errors.py
@tests/test_datasette_client_retry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QueryTimeoutError subclass + httpx.TimeoutException catch in _request_with_retry</name>
  <files>src/mcp_zeeker/core/datasette_client.py</files>
  <read_first>
    - src/mcp_zeeker/core/datasette_client.py lines 24–42 (existing UpstreamCallFailed class — QueryTimeoutError subclasses it)
    - src/mcp_zeeker/core/datasette_client.py lines 126–158 (existing `_request_with_retry` — TimeoutException catch goes BEFORE the existing RequestError catch because TimeoutException is a subclass of RequestError)
    - src/mcp_zeeker/core/errors.py (from 07-04 — confirms the catalog name `query_timeout` for documentation)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Open Questions #3 (recommended approach: catch TimeoutException before RequestError, raise QueryTimeoutError subclass)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § core/datasette_client.py (modified — TimeoutException distinction) for the exact catch-clause shape
  </read_first>
  <action>
    In src/mcp_zeeker/core/datasette_client.py, immediately AFTER the `UpstreamCallFailed` class definition (line ~42), add a new class `class QueryTimeoutError(UpstreamCallFailed): pass` with a one-paragraph docstring: "Raised when httpx.TimeoutException is caught inside _request_with_retry. Subclass of UpstreamCallFailed so existing handlers that catch UpstreamCallFailed continue to work; tool handlers can distinguish via isinstance(exc, QueryTimeoutError) and emit the `query_timeout` catalog code (per core/errors.py)." In `_request_with_retry` (line 126), modify the existing exception handling. Currently the try block at line 136–140 catches `httpx.RequestError`. Replace those four lines with: a) `except httpx.TimeoutException as exc: raise QueryTimeoutError(str(exc)) from exc`; b) `except httpx.RequestError as exc: raise UpstreamCallFailed(str(exc)) from exc`. Order matters: `httpx.TimeoutException` is a subclass of `httpx.RequestError`, so the more-specific exception must be caught first. Preserve the inline `# D-16: no retry on transport errors in Phase 1` comment, re-position it above the RequestError branch since that branch is what implements that decision; add a parallel `# ERR-04 / Q-OPEN-3: timeouts get their own QueryTimeoutError subclass so handlers can map to `query_timeout` catalog code` comment above the TimeoutException branch. No other changes to the function body — the retry logic, the 504 immediate-raise, the exhaustion final-raise are all already correct per RESEARCH.md § 502/503 Retry Verification.
  </action>
  <acceptance_criteria>
    - `python -c "from mcp_zeeker.core.datasette_client import QueryTimeoutError, UpstreamCallFailed; assert issubclass(QueryTimeoutError, UpstreamCallFailed)"` exits 0.
    - `grep -c 'class QueryTimeoutError' src/mcp_zeeker/core/datasette_client.py` outputs `1`.
    - `grep -c 'httpx.TimeoutException' src/mcp_zeeker/core/datasette_client.py` outputs `1`.
    - The catch order is correct: `grep -n 'except httpx' src/mcp_zeeker/core/datasette_client.py` shows the TimeoutException line BEFORE the RequestError line (TimeoutException's line number < RequestError's line number).
    - `uv run ruff check src/mcp_zeeker/core/datasette_client.py` exits 0.
    - `uv run pytest tests/test_datasette_client_retry.py -x` exits 0 (no regression on existing tests — the existing test_504 and the 502/503 retry-then-succeed cases must still pass; they don't hit the TimeoutException branch).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_datasette_client_retry.py -x</automated>
  </verify>
  <done>QueryTimeoutError subclass exists; TimeoutException is caught BEFORE RequestError; existing retry tests still GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add exhaustion + timeout tests, align names with VALIDATION.md</name>
  <files>tests/test_datasette_client_retry.py</files>
  <read_first>
    - tests/test_datasette_client_retry.py (full file — existing fixture + test patterns lines 1–89)
    - src/mcp_zeeker/core/datasette_client.py (just modified — verify the new QueryTimeoutError import path)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § tests/test_datasette_client_retry.py (modified — exhaustion cases) for the exact exhaustion-pattern + asyncio.sleep mock
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § 502/503 Retry Verification (recommended new test names: test_502_twice_raises_upstream_unavailable, test_503_twice_raises, etc.)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Per-Task Verification Map (the names the verifier expects: `test_502_twice_raises`, `test_504_raises_immediately`)
  </read_first>
  <action>
    Open tests/test_datasette_client_retry.py. The existing test `test_504_raises_immediately_no_retry` (line 78–89) covers the same property as VALIDATION.md's `test_504_raises_immediately` but the name differs. Rename it to `test_504_raises_immediately` (drop the `_no_retry` suffix) so the VALIDATION.md test map line for ERR-04 504 stays GREEN by name. Update any internal docstring references. Add a NEW test `test_502_twice_raises` following the analog pattern from `test_502_retries_once_then_succeeds` (lines 44–58). Body: register `httpx_mock.add_response(status_code=502)` twice; wrap in `with patch.object(asyncio, "sleep", new_callable=AsyncMock) as mock_sleep:` and inside `with pytest.raises(UpstreamCallFailed, match="retry exhausted"):` call `await client._request_with_retry("GET", "/test.json")`. After the block, assert `mock_sleep.call_count == 1` (one retry sleep between the two attempts), assert the sleep arg is in [0.25, 0.50], assert `len(httpx_mock.get_requests()) == 2` (no third attempt). Add a symmetric `test_503_twice_raises` with the same shape but status_code=503. Add a fourth test `test_timeout_raises_query_timeout_error` that proves the new TimeoutException branch fires. Use the pytest-httpx `httpx_mock.add_exception(httpx.ReadTimeout("simulated timeout"))` pattern (httpx.ReadTimeout is a subclass of TimeoutException). Body: `httpx_mock.add_exception(httpx.ReadTimeout("simulated timeout"))`; `with patch.object(asyncio, "sleep", new_callable=AsyncMock) as mock_sleep:` then `with pytest.raises(QueryTimeoutError):` call `await client._request_with_retry("GET", "/test.json")`. Assert `mock_sleep.call_count == 0` (no retry on transport errors per D-16 — the TimeoutException branch raises immediately, same as the existing RequestError branch). Imports needed: `from mcp_zeeker.core.datasette_client import DatasetteClient, UpstreamCallFailed, QueryTimeoutError`. Do not modify conftest.py (single-plan-touch rule preserved).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_datasette_client_retry.py::test_504_raises_immediately -x` exits 0.
    - `uv run pytest tests/test_datasette_client_retry.py::test_502_twice_raises -x` exits 0.
    - `uv run pytest tests/test_datasette_client_retry.py::test_503_twice_raises -x` exits 0.
    - `uv run pytest tests/test_datasette_client_retry.py::test_timeout_raises_query_timeout_error -x` exits 0.
    - `uv run pytest tests/test_datasette_client_retry.py -x` exits 0 (full module GREEN; the pre-existing `test_2xx_returns_immediately`, `test_502_retries_once_then_succeeds`, `test_503_retries_once_then_succeeds` MUST also still pass).
    - `grep -c 'def test_504_raises_immediately' tests/test_datasette_client_retry.py` outputs `1` (renamed) and `grep -c 'def test_504_raises_immediately_no_retry' tests/test_datasette_client_retry.py` outputs `0` (old name removed).
    - `uv run pytest -x` exits 0 (full suite GREEN).
    - `git diff tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_datasette_client_retry.py::test_502_twice_raises tests/test_datasette_client_retry.py::test_503_twice_raises tests/test_datasette_client_retry.py::test_504_raises_immediately tests/test_datasette_client_retry.py::test_timeout_raises_query_timeout_error -x</automated>
  </verify>
  <done>Four ERR-04 test names align with VALIDATION.md exactly; exhaustion case and timeout-distinction case proven; existing retry-then-succeed tests preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream Datasette (data.zeeker.sg) → DatasetteClient | Untrusted in availability; retry-once-with-jitter on 502/503; immediate raise on 504/timeouts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | Upstream body echo in error path | mitigate | The exhaustion path raises `UpstreamCallFailed(f"upstream retry exhausted on {url}")` — `url` is server-constructed from validated path components (database/table identifiers), never user-supplied filter values. Tool handlers map UpstreamCallFailed to `raise_upstream_unavailable()` (from 07-04) which discards the constructor message in favor of a FIXED literal. INJ-05 chain preserved end-to-end. |
</threat_model>

<verification>
- `uv run pytest tests/test_datasette_client_retry.py -x` is GREEN with 4 new/renamed test names visible in collection.
- `uv run pytest -x` is GREEN.
- `python -c "from mcp_zeeker.core.datasette_client import QueryTimeoutError; print('ok')"` prints `ok`.
- `grep -c 'except httpx.TimeoutException' src/mcp_zeeker/core/datasette_client.py` outputs `1`.
</verification>

<success_criteria>
- `QueryTimeoutError(UpstreamCallFailed)` subclass exists in `core/datasette_client.py`.
- `httpx.TimeoutException` is caught BEFORE `httpx.RequestError` in `_request_with_retry`.
- Test names `test_502_twice_raises`, `test_503_twice_raises`, `test_504_raises_immediately`, `test_timeout_raises_query_timeout_error` exist and are GREEN.
- No regression on existing retry tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-05-SUMMARY.md` documenting the new `QueryTimeoutError` subclass placement, the catch-clause re-ordering, the renamed `test_504_raises_immediately` (old name and new name), and the four ERR-04 tests now matching VALIDATION.md by name.
</output>
