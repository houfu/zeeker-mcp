---
phase: 07
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/mcp_zeeker/core/middleware/rate_limit.py
  - tests/test_rate_limit.py
autonomous: true
requirements:
  - RATE-01
  - RATE-02
  - RATE-05
must_haves:
  truths:
    - "A sustained-rate request, sent at 1-second intervals indefinitely, never produces a 429 because the bucket refills exactly one token per second."
    - "The 5001st request from one IP within a single UTC day returns 429 even when the burst bucket has tokens."
    - "When the UTC date advances (fake clock pushed past midnight), the daily counter resets to zero on the next request."
    - "When both burst and daily windows are exhausted simultaneously, Retry-After equals the LARGER of the two waits in integer seconds (D7-02)."
    - "The 21st request short-circuits ahead of FastMCP — a deliberately malformed JSON-RPC body still produces an HTTP 429, not a JSON-RPC parse error."
  artifacts:
    - path: tests/test_rate_limit.py
      provides: "GREEN tests for sustained refill, daily ceiling, UTC midnight reset, multi-window max-of-waits, and RATE-02 short-circuit"
      contains: "test_sustained_refill_after_one_second"
  key_links:
    - from: tests/test_rate_limit.py
      to: src/mcp_zeeker/core/middleware/rate_limit.py
      via: "Drives _check_bucket directly with fake_clock + datetime variants; drives __call__ via captured-send for the RATE-02 short-circuit test"
      pattern: "_check_bucket"
---

<objective>
Extend the 07-01 rate-limit middleware from single-dimension burst enforcement to the full three-window contract (burst + sustained refill + daily ceiling), enforce D7-01 UTC-midnight daily reset, and enforce D7-02 max-of-waits Retry-After arithmetic when multiple windows are exhausted.

Purpose: After this plan, an MCP client OBSERVES the full RATE-01 token bucket — burst tokens refill at 1/s sustained, hitting 5,000 requests in one UTC day produces 429 even when burst tokens remain, the daily counter resets cleanly at the next UTC midnight, and Retry-After always reflects the longest active wait so a well-behaved client never re-trips immediately. The middleware module body from 07-01 already implements all of this; this plan is the test-driven contract lock.
Output: Five additional GREEN tests in `tests/test_rate_limit.py`; any minor bug fixes in `core/middleware/rate_limit.py` that the new tests surface.
</objective>

## Phase Goal

**As an** MCP client of mcp.zeeker.sg, **I want to** know that my burst, sustained, and daily quotas all reset on a predictable UTC schedule and that the Retry-After value reflects the longest of the active waits, **so that** my client back-off honors every active window without immediately re-tripping the limiter.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-01-SUMMARY.md
@src/mcp_zeeker/core/middleware/rate_limit.py
@tests/conftest.py
@tests/test_rate_limit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GREEN sustained refill + daily ceiling + UTC midnight reset tests</name>
  <files>tests/test_rate_limit.py</files>
  <read_first>
    - src/mcp_zeeker/core/middleware/rate_limit.py (full file — verify `_check_bucket` daily counter / refill / daily_exceeded transitions match what the new tests assert)
    - tests/conftest.py § Phase 7 — Rate limit fixtures (added in 07-01; tests consume `rate_limiter`, `fake_clock`, `bucket_store`)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Token Bucket Math (refill formula, daily reset semantics, integer Retry-After invariant)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md § Decisions D7-01 (UTC midnight reset)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Per-Task Verification Map (test names this plan GREENs)
  </read_first>
  <action>
    Remove the `@pytest.mark.skip` decorators from `test_sustained_refill_after_one_second`, `test_daily_limit_5000`, `test_daily_reset_at_utc_midnight`, and `test_retry_after_is_integer` and fill their bodies. All four tests use the `rate_limiter` + `fake_clock` + `bucket_store` fixtures established in 07-01 and exercise `rate_limiter._check_bucket(key, fake_clock[0], <datetime>)` directly. (1) `test_sustained_refill_after_one_second`: drive 20 calls to drain the burst at `fake_clock[0]=0.0, datetime(2026,1,1,12,0,tzinfo=UTC)`; the 21st at the same `now_mono` returns `(False, ...)`. Advance `fake_clock[0] = 1.0` and assert the next call returns `(True, 0)`. Advance again to `fake_clock[0] = 2.0` and assert another `(True, 0)`. (2) `test_daily_limit_5000`: drive 5000 successful calls — alternating `fake_clock[0]` between sustained-friendly intervals so burst is never depleted (e.g., advance by 1.0 each call after the first 20, so tokens refill in lock-step). Assert the 5001st call returns `(False, retry_after)` with `retry_after >= 1`. Verify `bucket_store[key].daily_count == 5000` after the rejection and `bucket_store[key].daily_exceeded is True`. (3) `test_daily_reset_at_utc_midnight`: drive 5000 successful calls on UTC date 2026-01-01; verify the 5001st call (still on 2026-01-01) is rejected; pass `datetime(2026,1,2,0,0,1,tzinfo=UTC)` as the now_utc argument to the next `_check_bucket` call and assert it returns `(True, 0)` and `bucket_store[key].daily_count == 1` and `bucket_store[key].daily_date == date(2026, 1, 2)` and `bucket_store[key].daily_exceeded is False`. (4) `test_retry_after_is_integer`: trigger a rejection by draining the burst and assert `isinstance(retry_after, int) and retry_after >= 1`. Then drain the daily counter to exhaustion (same setup as test 2) and assert the rejection's `retry_after` is also `isinstance(retry_after, int) and retry_after >= 1` (a positive integer — never zero or a float). Imports needed at the top of the file (if not already present from 07-01): `from datetime import UTC, date, datetime`. Do not add any new fixtures to conftest.py (single-plan-touch rule).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rate_limit.py::test_sustained_refill_after_one_second -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_daily_limit_5000 -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_daily_reset_at_utc_midnight -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_retry_after_is_integer -x` exits 0.
    - `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` decreases by exactly 4 compared to the 07-01 SUMMARY.
    - `git diff tests/conftest.py` is empty (no conftest changes).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rate_limit.py::test_sustained_refill_after_one_second tests/test_rate_limit.py::test_daily_limit_5000 tests/test_rate_limit.py::test_daily_reset_at_utc_midnight tests/test_rate_limit.py::test_retry_after_is_integer -x</automated>
  </verify>
  <done>RATE-01 three-window contract is observable in tests; UTC midnight reset is exercised on the boundary; integer Retry-After invariant is asserted on both burst-only and daily-exhausted rejections.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN max-of-waits Retry-After + RATE-02 short-circuit-before-JSON-RPC tests</name>
  <files>tests/test_rate_limit.py</files>
  <read_first>
    - src/mcp_zeeker/core/middleware/rate_limit.py (full file — confirm `_check_bucket` deny-path implements `max(waits)`, and `__call__` writes the 429 response BEFORE delegating to self.app)
    - tests/test_app.py lines 22–73 (asgi_client pattern for end-to-end ASGI tests; for this task we use a minimal scope dict and captured-send pattern instead of asgi_client because asgi_client requires the full lifespan)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Retry-After Arithmetic (worked example: burst empty + daily exceeded → Retry-After = max(1, seconds_to_midnight))
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § 429 Transport Compatibility (RATE-02 must short-circuit before JSON-RPC parse)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md § Decisions D7-02 (Retry-After = max(active_window_waits))
  </read_first>
  <action>
    Un-skip and fill the bodies of `test_retry_after_max_of_windows` and `test_rate_limit_fires_before_json_rpc_parse`. (1) `test_retry_after_max_of_windows`: set up a bucket that has BOTH `tokens < 1.0` AND `daily_exceeded is True`. The cleanest setup: drive 5000 successful calls (same lock-step pattern as `test_daily_limit_5000`) so the daily counter is exhausted; ensure the last call also drained `tokens` (set `fake_clock[0]` such that the final refill leaves `tokens < 1.0`). At `now_utc = datetime(2026, 1, 1, 23, 55, 0, tzinfo=UTC)` (5 minutes before midnight per RESEARCH.md worked example), call `_check_bucket` — assert `(False, retry_after)` where `retry_after == 300` (seconds to midnight is the larger wait — burst wait is 1, daily wait is 300, max is 300). Also assert that with `now_utc = datetime(2026, 1, 2, 23, 59, 0, tzinfo=UTC)` after a fresh exhaustion the value matches `max(1, seconds_to_utc_midnight)`. Document the test name maps directly to D7-02 in a docstring. (2) `test_rate_limit_fires_before_json_rpc_parse`: this test must use the full ASGI machinery — drive `rate_limiter(scope, receive, send)` 21 times in a single test with a scope whose body is a deliberately malformed JSON-RPC payload `b'\\x00\\x01NOT VALID JSON-RPC\\x02\\x03'` and `headers = [(b"content-type", b"application/json")]`. Use the captured-send pattern from 07-01 task 3. The 21st call should produce an `http.response.start` with `status == 429` AND the JSON body should be parseable as `{"error": {"code": "rate_limited", ...}}` — proving the 429 fired without parsing the JSON-RPC body. Assert that even with a malformed body, no exception is raised from `await rate_limiter(scope, receive, send)` (because the rate-limit middleware never reads the body itself).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rate_limit.py::test_retry_after_max_of_windows -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_rate_limit_fires_before_json_rpc_parse -x` exits 0.
    - `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` decreases by exactly 2 relative to the post-Task-1 state.
    - `uv run pytest tests/test_rate_limit.py -x` exits 0 (no regression on previously GREEN tests).
    - `uv run pytest -x` exits 0 (full suite still GREEN).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rate_limit.py::test_retry_after_max_of_windows tests/test_rate_limit.py::test_rate_limit_fires_before_json_rpc_parse -x</automated>
  </verify>
  <done>Multi-window max(waits) Retry-After verified for the burst+daily combination on a realistic 5-min-before-midnight scenario; RATE-02 short-circuit-before-JSON-RPC-parse proven against a deliberately malformed body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASGI middleware → FastMCP JSON-RPC parser | Rate-limit must short-circuit on 21st request without reading the request body |
| Multi-window arithmetic → Retry-After header | Both burst and daily wait must be computed independently and max()-ed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `_check_bucket` multi-window arithmetic | mitigate | `test_retry_after_max_of_windows` exercises the burst+daily combination at the 23:55 UTC boundary so a regression in `max(waits)` flips the assertion |
| T-07-02 | Elevation of Privilege | Daily counter reset boundary | mitigate | `test_daily_reset_at_utc_midnight` advances the fake `now_utc` past midnight and asserts `daily_count` resets to 1 on the next call and `daily_date` updates to the new UTC date; sticky-TTL eviction is plan 07-03's responsibility |
| T-07-03 | Spoofing | RATE-02 placement (must run before JSON-RPC parse) | mitigate | `test_rate_limit_fires_before_json_rpc_parse` drives the middleware with a deliberately malformed JSON-RPC body; 429 fires without an exception, proving the body is never parsed |
</threat_model>

<verification>
- `uv run pytest tests/test_rate_limit.py -x` is GREEN.
- `uv run pytest -x` is GREEN.
- `git diff tests/conftest.py` is empty (single-plan-touch rule preserved).
- `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` shows 6 fewer skips than after 07-01.
</verification>

<success_criteria>
- Sustained 1/s refill is exercised in tests.
- Daily ceiling of 5,000 is exercised in tests.
- Daily counter resets cleanly at the next UTC midnight via fake clock.
- Retry-After = max(burst_wait, daily_wait) verified on the burst+daily combined-exhaustion path.
- RATE-02 short-circuit-before-JSON-RPC-parse proven with a deliberately malformed payload.
- No regression on plan 07-01's three GREEN tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-02-SUMMARY.md` documenting which tests transitioned from skip to GREEN, the exact `(now_mono, now_utc)` schedules used in the daily-counter and max-of-waits tests, and any bugs surfaced in `core/middleware/rate_limit.py` along with the fix.
</output>
