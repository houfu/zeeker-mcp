---
phase: 07
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/mcp_zeeker/core/middleware/rate_limit.py
  - tests/test_rate_limit.py
autonomous: true
requirements:
  - RATE-03
  - RATE-04
must_haves:
  truths:
    - "Right-to-left XFF parsing with TRUSTED_PROXY_DEPTH=1 returns the spoofed client position when XFF has multiple hops."
    - "XFF with fewer hops than depth falls back to the leftmost present entry (no out-of-range index access)."
    - "A single TCP peer cannot expand the bucket store beyond RATE_STORE_CAP (100k) via XFF spoofing — store size is hard-capped via batch LRU eviction."
    - "A daily-locked bucket is not evicted by the 15-minute idle TTL — sticky TTL = max(15 min, time-to-next-utc-midnight) keeps it pinned until the day rolls over."
  artifacts:
    - path: src/mcp_zeeker/core/middleware/rate_limit.py
      provides: "Filled-in _sweep method + _enforce_cap batch LRU eviction + sticky-TTL idle calculation"
      contains: "def _sweep"
    - path: tests/test_rate_limit.py
      provides: "GREEN tests for XFF depth=1, XFF fewer-hops fallback, store cap under flood, sticky-TTL pinning"
      contains: "test_store_cap_enforced_under_flood"
  key_links:
    - from: src/mcp_zeeker/core/middleware/rate_limit.py
      to: src/mcp_zeeker/core/ip.py
      via: "client_ip_from_scope already wired; this plan adds NO new IP-parsing code, only tests"
      pattern: "client_ip_from_scope"
    - from: tests/test_rate_limit.py
      to: src/mcp_zeeker/core/middleware/rate_limit.py
      via: "Drives __call__ with synthetic XFF + drives _sweep + _enforce_cap directly"
      pattern: "_enforce_cap"
---

<objective>
Complete the bucket store eviction story: implement the `_sweep` (idle-TTL pruning with sticky TTL for daily-locked buckets) and `_enforce_cap` (batch LRU eviction at 100k cap) methods left as stubs in 07-01, then prove via tests that the store is bounded under hostile XFF flood and that daily-locked buckets cannot evade their daily ceiling by going idle for 15 minutes.

Purpose: After this plan, an MCP client OBSERVES the full RATE-03 + RATE-04 contract — XFF parsing is honest at depth=1, a 10k-IP spoof attack cannot exhaust server memory, and a daily-locked legitimate IP cannot bypass its quota by waiting out the standard idle TTL. This closes the rate-limit security envelope.
Output: Filled-in `_sweep` + `_enforce_cap` methods in `core/middleware/rate_limit.py`; four GREEN tests in `tests/test_rate_limit.py` (XFF depth, XFF fewer-hops, store cap, sticky TTL).
</objective>

## Phase Goal

**As the** operator of mcp.zeeker.sg behind one Caddy hop, **I want to** know that an attacker spoofing 100,000 distinct X-Forwarded-For values from one TCP peer cannot exhaust my process memory, AND that an attacker who has burned their daily ceiling cannot reset it by going idle for 15 minutes, **so that** the in-memory rate-limit envelope holds up against adversarial flood patterns.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-01-SUMMARY.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-02-SUMMARY.md
@src/mcp_zeeker/core/middleware/rate_limit.py
@src/mcp_zeeker/core/ip.py
@tests/conftest.py
@tests/test_rate_limit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement _sweep (sticky TTL) and _enforce_cap (batch LRU) in RateLimitMiddleware</name>
  <files>src/mcp_zeeker/core/middleware/rate_limit.py</files>
  <read_first>
    - src/mcp_zeeker/core/middleware/rate_limit.py (full file — `_sweep` is a stub left by 07-01 task 2; `__call__` already invokes it under the time-gated check, this task fills the body and adds `_enforce_cap`)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Bucket Store + Eviction (sticky-TTL algorithm, `_effective_ttl`, `_is_expired`, `sweep`, and batch `_enforce_cap` reference implementations)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md § Decisions D7-03 (sticky TTL for daily-locked buckets — max(15 min, time-to-next-utc-midnight))
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § core/middleware/rate_limit.py (divergence-from-analog note: stateful per-IP store is intentional)
  </read_first>
  <action>
    In src/mcp_zeeker/core/middleware/rate_limit.py, add a method `_effective_ttl(self, bucket: BucketState, now_utc: datetime) -> float` implementing D7-03: when `bucket.daily_exceeded is True` return `max(self._idle_ttl_seconds, float(self._seconds_to_utc_midnight(now_utc)))`; otherwise return `self._idle_ttl_seconds`. Add `_is_expired(self, bucket: BucketState, now_mono: float, now_utc: datetime) -> bool` returning `(now_mono - bucket.last_seen_ts) > self._effective_ttl(bucket, now_utc)`. Replace the `_sweep` stub body (currently `pass`) with: build `expired = [key for key, bucket in self._store.items() if self._is_expired(bucket, now_mono, now_utc)]`, then `for key in expired: del self._store[key]`. Add a new method `_enforce_cap(self, now_mono: float, now_utc: datetime) -> None` per RESEARCH.md § Bucket Store + Eviction "Recommended LRU eviction batch": if `len(self._store) >= self._store_cap`, compute `evict_count = max(1, len(self._store) // 100)` (1 % of cap = 1,000 entries when cap = 100k), sort keys by `self._store[key].last_seen_ts` ascending, delete the first `evict_count` keys. Call `self._enforce_cap(now_mono, now_utc)` at the END of `_check_bucket`'s create-new-entry branch (right after inserting the new entry) — this is the only path where `len(self._store)` grows, so it is the right gate. Do NOT call `_enforce_cap` inside `_sweep` (separating concerns: `_sweep` is idle-based pruning, `_enforce_cap` is cap-based pruning). Keep `__call__`'s existing time-gated `_sweep` invocation; add `# RATE-04: batch LRU backstop fires only when the store crosses RATE_STORE_CAP — under normal load it never runs.` as an inline comment on `_enforce_cap`. Document at the top of the file (module docstring) that under a >100k unique-attacker-IP simultaneous flood, a legitimate daily-locked IP could be evicted by LRU and start a fresh daily counter on re-entry — this is an accepted tradeoff per RESEARCH.md § Bucket Store + Eviction "Critical correctness invariant".
  </action>
  <acceptance_criteria>
    - `grep -c 'def _sweep' src/mcp_zeeker/core/middleware/rate_limit.py` outputs `1`.
    - `grep -c 'def _enforce_cap' src/mcp_zeeker/core/middleware/rate_limit.py` outputs `1`.
    - `grep -c 'def _effective_ttl' src/mcp_zeeker/core/middleware/rate_limit.py` outputs `1`.
    - `grep -c 'def _is_expired' src/mcp_zeeker/core/middleware/rate_limit.py` outputs `1`.
    - `grep -v '^#' src/mcp_zeeker/core/middleware/rate_limit.py | grep -c 'last_seen_ts'` is `>= 3` (used in _is_expired, _enforce_cap, and the bucket-creation/update paths).
    - `python -c "from mcp_zeeker.core.middleware.rate_limit import RateLimitMiddleware; m = RateLimitMiddleware(lambda *a: None, burst=20, sustained_per_second=1.0, daily_limit=5000, store_cap=10, idle_ttl_seconds=900.0); print('ok')"` exits 0.
    - `uv run pytest tests/test_rate_limit.py -x` still GREEN (no regression on existing tests).
    - `uv run ruff check src/mcp_zeeker/core/middleware/rate_limit.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/mcp_zeeker/core/middleware/rate_limit.py</automated>
  </verify>
  <done>Four eviction-related methods implemented; bucket-creation path calls _enforce_cap; module docstring documents the >100k-attacker-IP tradeoff; no existing test regresses.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN XFF parsing + store cap + sticky TTL tests</name>
  <files>tests/test_rate_limit.py</files>
  <read_first>
    - src/mcp_zeeker/core/middleware/rate_limit.py (full file — verify `_check_bucket`, `_sweep`, `_enforce_cap`, `_effective_ttl` behavior the tests will assert)
    - src/mcp_zeeker/core/ip.py (full file — verify `client_ip_from_scope` extracts position `-(depth+1)` when XFF has multiple hops, and `parts[0]` when len <= depth)
    - tests/conftest.py § Phase 7 — Rate limit fixtures (consume `rate_limiter`, `fake_clock`, `bucket_store`)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § XFF Parsing § Edge Cases Handled (truth table for the depth-1 case)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Bucket Store + Eviction § 10k Spoofed-XFF Flood Walkthrough (test scenario)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Per-Task Verification Map (test names this plan GREENs)
  </read_first>
  <action>
    Un-skip and fill the bodies of `test_xff_parsing_depth_1`, `test_xff_fewer_hops_than_depth`, `test_store_cap_enforced_under_flood`, and `test_sticky_ttl_daily_locked_not_expired`. (1) `test_xff_parsing_depth_1`: build a scope with `headers=[(b"x-forwarded-for", b"203.0.113.5, 10.0.0.1")]` and `client=("10.0.0.1", 443)`. Drive `await rate_limiter(scope, receive, send)` once with the captured-send pattern. Assert that `bucket_store` has exactly one entry whose key is `"203.0.113.5"` — the leftmost entry per the depth=1 algorithm in `core/ip.py:client_ip_from_scope`. (2) `test_xff_fewer_hops_than_depth`: build a scope with `headers=[(b"x-forwarded-for", b"203.0.113.5")]` (a single XFF hop, fewer than depth=1). Drive one request. Assert `bucket_store` has exactly one entry keyed `"203.0.113.5"` (the leftmost-present entry, per `parts[0] if len(parts) <= depth`). (3) `test_store_cap_enforced_under_flood`: instantiate a fresh `RateLimitMiddleware` directly in the test with `store_cap=50` (NOT 100k — keep the test fast) and the same other constants from conftest. Drive `__call__` with 200 unique spoofed XFF values (e.g., `f"198.51.100.{i}"` for i in range(200)) at advancing `fake_clock[0]` so each call creates a new bucket. After the loop, assert `len(middleware._store) <= 50`. (4) `test_sticky_ttl_daily_locked_not_expired`: set `fake_clock[0] = 0.0`, `now_utc = datetime(2026, 1, 1, 12, 0, tzinfo=UTC)`. Manually drive `_check_bucket` 5000 times to drive a single key into `daily_exceeded=True` state (re-use the lock-step pattern from 07-02 task 1, or directly mutate `bucket_store[key].daily_count = 5000; bucket_store[key].daily_exceeded = True; bucket_store[key].daily_date = date(2026,1,1); bucket_store[key].last_seen_ts = 0.0` for test efficiency — both are acceptable). Advance `fake_clock[0] = 901.0` (just past the 15-min standard TTL) but keep `now_utc = datetime(2026, 1, 1, 12, 15, 1, tzinfo=UTC)` (still 2026-01-01, ~11h 45min before next UTC midnight). Manually call `rate_limiter._sweep(901.0, datetime(2026,1,1,12,15,1,tzinfo=UTC))`. Assert the daily-locked bucket is STILL present in `bucket_store` — the sticky TTL (`max(900, ~42300) == 42300`) keeps it pinned. Then advance to the next UTC day: call `_sweep(<much later>, datetime(2026, 1, 2, 0, 0, 1, tzinfo=UTC))` after setting `bucket_store[key].last_seen_ts = 0.0` (i.e., the bucket has been idle a long time AND we're past midnight) — assert it is now evicted because the next-midnight wait is now <15min and the daily counter resets are a separate path (the bucket is not RE-locked unless a new request comes in). This second sub-assertion verifies that sticky-TTL only applies WHILE `daily_exceeded` is true on the current day — once midnight crosses, the bucket's effective TTL collapses back to 15 min so eviction can proceed.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rate_limit.py::test_xff_parsing_depth_1 -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_xff_fewer_hops_than_depth -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_store_cap_enforced_under_flood -x` exits 0.
    - `uv run pytest tests/test_rate_limit.py::test_sticky_ttl_daily_locked_not_expired -x` exits 0.
    - `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` decreases by exactly 4 relative to the post-07-02 state.
    - `uv run pytest tests/test_rate_limit.py -x` exits 0 (no regression).
    - `uv run pytest -x` exits 0 (full suite GREEN).
    - `git diff tests/conftest.py` is empty (single-plan-touch rule preserved).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rate_limit.py::test_xff_parsing_depth_1 tests/test_rate_limit.py::test_xff_fewer_hops_than_depth tests/test_rate_limit.py::test_store_cap_enforced_under_flood tests/test_rate_limit.py::test_sticky_ttl_daily_locked_not_expired -x</automated>
  </verify>
  <done>RATE-03 XFF semantics and RATE-04 store cap + sticky TTL pinning all proven in tests; store-cap test uses a small cap (50) for speed; sticky-TTL test exercises both the "pin" case and the "release after midnight" case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attacker → Caddy → MCP container | XFF can be spoofed for entries to the LEFT of Caddy's append; Caddy's appended entry is trusted at depth=1 |
| In-memory bucket store | Bounded by RATE_STORE_CAP=100k; batch LRU at cap, time-gated sweep for idle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | XFF parsing depth | mitigate | `test_xff_parsing_depth_1` and `test_xff_fewer_hops_than_depth` lock the right-to-left, depth=1 algorithm shared with `core/ip.py:client_ip`; deviation from depth=1 in production (multi-hop CDN) requires an explicit `TRUSTED_PROXY_DEPTH` change in config |
| T-07-05 | Denial of Service | Bucket store size | mitigate | `_enforce_cap` batch-LRU evicts oldest 1% when `len(store) >= RATE_STORE_CAP`; `test_store_cap_enforced_under_flood` proves the cap holds under a 200-IP flood against a `store_cap=50` test fixture |
| T-07-06 | Elevation of Privilege | Daily-lock evasion via 15-min idle | mitigate | `_effective_ttl` returns `max(15 min, seconds_to_next_utc_midnight)` when `bucket.daily_exceeded is True`; `test_sticky_ttl_daily_locked_not_expired` proves a daily-locked bucket survives 15-min idle on the same UTC day; documented tradeoff: >100k simultaneous unique attacker IPs CAN evict a legitimate daily-locked bucket (accepted, documented in module docstring) |
</threat_model>

<verification>
- `uv run pytest tests/test_rate_limit.py -x` is GREEN.
- `uv run pytest -x` is GREEN.
- `grep -c '@pytest.mark.skip' tests/test_rate_limit.py` shows 4 fewer skips than after 07-02.
- `git diff tests/conftest.py` is empty.
- Module docstring of `core/middleware/rate_limit.py` documents the >100k-unique-IP tradeoff.
</verification>

<success_criteria>
- `_sweep` and `_enforce_cap` are implemented with the algorithms cited in RESEARCH.md § Bucket Store + Eviction.
- XFF depth=1 parsing is exercised in two flavors (multi-hop and fewer-hops-than-depth).
- Store cap is enforced under flood (test fixture uses cap=50 for speed; production uses 100,000).
- Daily-locked buckets survive standard 15-min idle TTL on the same UTC day.
- No regression on plans 07-01 and 07-02's GREEN tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-03-SUMMARY.md` documenting the four method signatures added, the cap-fixture size (50) chosen for the flood test vs production (100k), and any edge cases in sticky-TTL behavior surfaced during testing.
</output>
