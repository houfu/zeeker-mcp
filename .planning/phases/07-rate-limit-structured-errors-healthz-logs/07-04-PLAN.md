---
phase: 07
plan: 04
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/mcp_zeeker/core/errors.py
  - src/mcp_zeeker/core/middleware/error_enrichment.py
  - src/mcp_zeeker/server.py
  - tests/test_error_catalog.py
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-05
must_haves:
  truths:
    - "An MCP client receiving a ToolError sees a message that starts with one of the 11 canonical catalog codes (e.g., 'unknown_database: ...', 'rate_limited' is the body-only code for ASGI 429 — included in the catalog constant for completeness)."
    - "Every ToolError surfaced through the FastMCP layer carries a [request_id: <hex>] suffix that an operator can use to correlate to a structured log line."
    - "Upstream Datasette 4xx (e.g., 400 FTS5 parse error) maps to a catalog code (`invalid_query` or `upstream_unavailable`) — never to a passthrough of the upstream message body (INJ-05 preserved)."
    - "The 11-code catalog is one literal source of truth at `core/errors.CATALOG`, mechanically asserted by `test_all_11_codes_in_catalog`."
  artifacts:
    - path: src/mcp_zeeker/core/errors.py
      provides: "CATALOG tuple constant + raise_query_timeout + raise_upstream_unavailable helpers"
      contains: "CATALOG"
    - path: src/mcp_zeeker/core/middleware/error_enrichment.py
      provides: "ErrorEnrichmentMiddleware FastMCP middleware that appends [request_id: ...] to ToolError messages"
      contains: "class ErrorEnrichmentMiddleware"
    - path: tests/test_error_catalog.py
      provides: "Catalog membership test + request_id suffix test + upstream 4xx no-echo test"
      contains: "test_all_11_codes_in_catalog"
  key_links:
    - from: src/mcp_zeeker/server.py
      to: src/mcp_zeeker/core/middleware/error_enrichment.py
      via: "mcp.add_middleware(ErrorEnrichmentMiddleware()) registered AFTER RetrievedAtMiddleware so retrieved_at remains bound during error enrichment"
      pattern: "ErrorEnrichmentMiddleware"
    - from: src/mcp_zeeker/core/middleware/error_enrichment.py
      to: src/mcp_zeeker/core/middleware/request_id.py
      via: "structlog.contextvars.get_contextvars().get('request_id', '') reads the contextvar bound upstream by RequestIdMiddleware"
      pattern: "get_contextvars"
---

<objective>
Lock the canonical 11-code error catalog into a single source-of-truth module (`core/errors.py`) and add the FastMCP ErrorEnrichmentMiddleware that appends `[request_id: <hex>]` to every ToolError message so operators can correlate client-side errors with server-side log lines (ERR-03).

Purpose: After this plan, an MCP client OBSERVES a stable contract — every ToolError carries one of 11 catalog codes as its `code:` prefix AND a `[request_id: ...]` suffix. The phase 3 six-code locked subset is extended to the full eleven (adding `unknown_database` confirmation, `invalid_query`, `query_timeout`, `rate_limited`, `upstream_unavailable`). Per RESEARCH.md Open Question #2, raise sites for `invalid_query` and `query_timeout` outside the existing Phase 4 search path are intentionally deferred (Phase 4 owns search; Phase 7 only adds the `query_timeout` raise site in 07-05).
Output: New `core/errors.py` module (catalog constant + raise_query_timeout / raise_upstream_unavailable helpers); new `core/middleware/error_enrichment.py` FastMCP middleware; FastMCP registration in `server.py`; three GREEN tests in `tests/test_error_catalog.py`.
</objective>

## Phase Goal

**As an** MCP client of mcp.zeeker.sg, **I want to** receive an error whose first colon-delimited token is one of 11 known codes AND a trailing `[request_id: <hex>]` that I can paste into an incident ticket, **so that** my retry logic and the operator's log search both work without ambiguity.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-CONTEXT.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md
@src/mcp_zeeker/core/visibility.py
@src/mcp_zeeker/core/middleware/retrieved_at.py
@src/mcp_zeeker/core/middleware/access_log.py
@src/mcp_zeeker/core/filter_compiler.py
@src/mcp_zeeker/server.py

<interfaces>
Existing raise helpers in `core/visibility.py` (DO NOT migrate; only extend):
```python
def raise_unknown_database(database: str) -> NoReturn: ...   # "unknown_database: Database not found: {database}"
def raise_unknown_table(database: str, table: str) -> NoReturn: ...  # "unknown_table: Table not found: {database}.{table}"
def raise_unknown_column(database: str, table: str, column: str) -> NoReturn: ...
def raise_unsupported_table_for_fetch(database: str, table: str) -> NoReturn: ...
def raise_not_found(database: str, table: str) -> NoReturn: ...
def raise_invalid_query() -> NoReturn: ...                   # "invalid_query: query syntax not supported"
```

Filter-compiler raise sites in `core/filter_compiler.py` (DO NOT migrate):
- `ToolError(f"unknown_column: Column not found: {f.column}")`
- `ToolError("invalid_filter_op: value required for this operator")`
- `ToolError("invalid_filter_op: in/notin value must be a flat list of str/int/float")`

Cursor raise sites in `tools/retrieval.py` (DO NOT migrate):
- `ToolError("invalid_cursor: ...")` (multiple sites)

FastMCP middleware shape from `core/middleware/retrieved_at.py`:
```python
class RetrievedAtMiddleware(Middleware):
    async def on_call_tool(self, context: MiddlewareContext, call_next):
        token = tool_started_at.set(datetime.now(tz=UTC))
        try:
            return await call_next(context)
        finally:
            tool_started_at.reset(token)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core/errors.py with CATALOG tuple + new raise helpers</name>
  <files>src/mcp_zeeker/core/errors.py</files>
  <read_first>
    - src/mcp_zeeker/core/visibility.py lines 19–108 (raise_* analog — exact docstring style, NoReturn return annotation, fixed-literal message convention for INJ-05-sensitive codes)
    - src/mcp_zeeker/core/filter_compiler.py lines 110–140 (existing ToolError raise sites — verifies the "code: message" pattern)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § core/errors.py (utility, none) for the exact module shape and divergence-from-analog notes
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Error Catalog Completion for the full 11-code table and INJ-05 verification per code
  </read_first>
  <action>
    Create src/mcp_zeeker/core/errors.py mirroring the import structure of visibility.py but trimmed: `from __future__ import annotations; from typing import NoReturn; from fastmcp.exceptions import ToolError`. Module docstring summarises this as the canonical Phase 7 catalog source (ERR-02), notes that existing raise helpers in `visibility.py` and `filter_compiler.py` remain in place (NOT migrated — they predate this module), and lists the four reasons-for-existence: (a) provide a single tuple of all 11 codes for `test_all_11_codes_in_catalog`; (b) provide canonical raise helpers for the two new Phase 7 codes (`query_timeout`, `upstream_unavailable` string form); (c) name `rate_limited` for completeness even though it's only emitted by the ASGI middleware body (never via ToolError); (d) document where each code is RAISED (file references). Define `CATALOG: tuple[str, ...] = ("unknown_database", "unknown_table", "unknown_column", "invalid_filter_op", "invalid_cursor", "invalid_query", "unsupported_table_for_fetch", "not_found", "query_timeout", "rate_limited", "upstream_unavailable")` — exact order matches the canonical order in REQUIREMENTS.md ERR-02 (preserve that ordering). Add `raise_query_timeout() -> NoReturn` raising `ToolError("query_timeout: Query timed out")` — FIXED literal, no URL or query echo (INJ-05). Add `raise_upstream_unavailable() -> NoReturn` raising `ToolError("upstream_unavailable: upstream call failed")` — FIXED literal, no upstream body echo (INJ-05 / ERR-05). Each helper has a one-paragraph docstring referencing the requirement ID and the upstream raise-site convention (e.g., `raise_upstream_unavailable` notes that tool handlers may instead raise `UpstreamCallFailed` and let the handler-level try/except map it; this helper exists for the canonical-string single-source-of-truth assertion in tests). Per PATTERNS.md § core/errors.py the EXISTING visibility.py helpers stay where they are — this is NEW co-existence, not migration.
  </action>
  <acceptance_criteria>
    - `python -c "from mcp_zeeker.core.errors import CATALOG; assert CATALOG == ('unknown_database', 'unknown_table', 'unknown_column', 'invalid_filter_op', 'invalid_cursor', 'invalid_query', 'unsupported_table_for_fetch', 'not_found', 'query_timeout', 'rate_limited', 'upstream_unavailable')"` exits 0.
    - `python -c "from mcp_zeeker.core.errors import CATALOG; assert len(CATALOG) == 11 and len(set(CATALOG)) == 11"` exits 0.
    - `python -c "from mcp_zeeker.core.errors import raise_query_timeout, raise_upstream_unavailable; print('ok')"` prints `ok`.
    - `python -c "from mcp_zeeker.core.errors import raise_query_timeout; from fastmcp.exceptions import ToolError; import pytest; pytest.raises(ToolError, raise_query_timeout)"` exits 0.
    - `grep -v '^#' src/mcp_zeeker/core/errors.py | grep -c '"query_timeout: Query timed out"'` outputs `1` (fixed literal — exactly one occurrence).
    - `grep -v '^#' src/mcp_zeeker/core/errors.py | grep -c '"upstream_unavailable: upstream call failed"'` outputs `1`.
    - `uv run ruff check src/mcp_zeeker/core/errors.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/mcp_zeeker/core/errors.py</automated>
  </verify>
  <done>core/errors.py compiles; CATALOG contains the 11 codes in REQUIREMENTS.md order; two new raise helpers exist with FIXED-literal messages per INJ-05.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ErrorEnrichmentMiddleware + register it in server.py</name>
  <files>src/mcp_zeeker/core/middleware/error_enrichment.py, src/mcp_zeeker/server.py</files>
  <read_first>
    - src/mcp_zeeker/core/middleware/retrieved_at.py (full file — analog FastMCP `on_call_tool` middleware shape; ErrorEnrichmentMiddleware uses try/except instead of try/finally per PATTERNS.md)
    - src/mcp_zeeker/core/middleware/access_log.py lines 7–43 (logger pattern; structlog usage pattern)
    - src/mcp_zeeker/server.py (full file — locate the `mcp.add_middleware(...)` call site for RetrievedAtMiddleware so ErrorEnrichment is registered AFTER it, preserving D6-10 ordering)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § core/middleware/error_enrichment.py (exact except-clause body, message re-raise pattern, request_id contextvar read)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-RESEARCH.md § Open Questions #1 (chosen approach: append `[request_id: ...]` to message string)
  </read_first>
  <action>
    Create src/mcp_zeeker/core/middleware/error_enrichment.py. Imports: `from __future__ import annotations; import structlog; from fastmcp.exceptions import ToolError; from fastmcp.server.middleware import Middleware, MiddlewareContext; from structlog.contextvars import get_contextvars`. Module-level `logger = structlog.get_logger()`. Module docstring explains: (a) this middleware closes ERR-03 (request_id correlation) by appending `[request_id: <hex>]` to every ToolError message so the client sees the same hex an operator can grep in logs; (b) the message-prefix convention is preserved — the catalog code is still the substring BEFORE the first `: `, so existing code-prefix detectors keep working; (c) it must be registered AFTER RetrievedAtMiddleware in `server.py` so RetrievedAt's `tool_started_at` contextvar remains bound during error handling. Class `ErrorEnrichmentMiddleware(Middleware)` with one async method `on_call_tool(self, context: MiddlewareContext, call_next)`. Body: `try: return await call_next(context)` then `except ToolError as exc:` — read `request_id = get_contextvars().get("request_id", "")`; if `request_id`, build `new_message = f"{exc.message} [request_id: {request_id}]"` and `raise ToolError(new_message) from exc`; if `request_id` is empty (test path with no RequestIdMiddleware bound), re-raise unmodified via `raise`. The `from exc` chain preserves the original exception for FastMCP/logging visibility. Do NOT catch base Exception — only ToolError, per scope of ERR-03. In src/mcp_zeeker/server.py, add `from mcp_zeeker.core.middleware.error_enrichment import ErrorEnrichmentMiddleware` near the existing middleware imports. Find the line that registers RetrievedAtMiddleware (`mcp.add_middleware(RetrievedAtMiddleware())` or similar — phase 6 set this up). Immediately AFTER that line, add `mcp.add_middleware(ErrorEnrichmentMiddleware())`. FastMCP middleware order is FIFO: RetrievedAt first (binds contextvar), ErrorEnrichment second (catches ToolError after the handler raises). Do not modify any other middleware order — D6-10 ordering preserved. Document the ordering choice in a comment on the new line: `# ERR-03: append [request_id: ...] to ToolError messages — registered AFTER RetrievedAt so retrieved_at stays bound during error handling.`
  </action>
  <acceptance_criteria>
    - `python -c "from mcp_zeeker.core.middleware.error_enrichment import ErrorEnrichmentMiddleware; print('ok')"` prints `ok`.
    - `grep -c 'class ErrorEnrichmentMiddleware' src/mcp_zeeker/core/middleware/error_enrichment.py` outputs `1`.
    - `grep -c 'except ToolError' src/mcp_zeeker/core/middleware/error_enrichment.py` outputs `1`.
    - `grep -c 'ErrorEnrichmentMiddleware' src/mcp_zeeker/server.py` is `>= 2` (import + add_middleware call).
    - `python -c "from mcp_zeeker import server; print('ok')"` prints `ok` (no import-time error after wiring).
    - `uv run ruff check src/mcp_zeeker/core/middleware/error_enrichment.py src/mcp_zeeker/server.py` exits 0.
    - `uv run pytest -x` exits 0 (no regression — existing tests run through both RetrievedAt and ErrorEnrichment).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -x</automated>
  </verify>
  <done>ErrorEnrichmentMiddleware module compiles; registered in FastMCP middleware chain AFTER RetrievedAtMiddleware; no regression on existing tests.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN catalog + request_id + upstream-4xx-no-echo tests</name>
  <files>tests/test_error_catalog.py</files>
  <read_first>
    - tests/test_filter_compiler.py (analog — ToolError-shape assertion pattern, pytest.raises usage)
    - tests/conftest.py (existing fixtures — no new fixtures added by this plan; single-plan-touch rule binds 07-01 only)
    - src/mcp_zeeker/core/errors.py (just created — CATALOG tuple + raise_query_timeout + raise_upstream_unavailable)
    - src/mcp_zeeker/core/visibility.py lines 19–108 (existing raise helpers — assertions verify they raise with the canonical "code: " prefix)
    - src/mcp_zeeker/core/middleware/error_enrichment.py (just created — verify request_id suffix appended on test path)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-PATTERNS.md § tests/test_error_catalog.py (exact test patterns)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VALIDATION.md § Per-Task Verification Map (test names this plan must emit)
  </read_first>
  <action>
    Create tests/test_error_catalog.py. Module docstring: "ERR-01..03 + ERR-05 — locked 11-code catalog, request_id correlation, upstream 4xx no-echo (INJ-05)". Imports: `from __future__ import annotations; import pytest; import structlog; from fastmcp.exceptions import ToolError; from structlog.contextvars import bind_contextvars, clear_contextvars; from mcp_zeeker.core.errors import CATALOG, raise_query_timeout, raise_upstream_unavailable; from mcp_zeeker.core.visibility import raise_unknown_database, raise_unknown_table, raise_unknown_column, raise_unsupported_table_for_fetch, raise_not_found, raise_invalid_query; from mcp_zeeker.core.middleware.error_enrichment import ErrorEnrichmentMiddleware`. Implement four tests with NO skip decorators (all GREEN immediately): (1) `test_all_11_codes_in_catalog` — assert `len(CATALOG) == 11` and `set(CATALOG) == {"unknown_database", "unknown_table", "unknown_column", "invalid_filter_op", "invalid_cursor", "invalid_query", "unsupported_table_for_fetch", "not_found", "query_timeout", "rate_limited", "upstream_unavailable"}` and the tuple preserves the REQUIREMENTS.md order. (2) `test_all_errors_have_stable_code` — iterate a list of `(raise_fn, args, expected_prefix)` tuples covering every CATALOG code that has a raise helper (10 of 11; `rate_limited` is body-only and covered in tests/test_rate_limit.py from 07-01). For each entry, call inside `pytest.raises(ToolError) as exc_info`, then assert `exc_info.value.message.startswith(f"{expected_prefix}: ")`. The list MUST include (`raise_unknown_database`, `("test-db",)`, `"unknown_database"`), (`raise_unknown_table`, `("db", "t")`, `"unknown_table"`), (`raise_unknown_column`, `("db", "t", "c")`, `"unknown_column"`), (`raise_unsupported_table_for_fetch`, `("db", "t")`, `"unsupported_table_for_fetch"`), (`raise_not_found`, `("db", "t")`, `"not_found"`), (`raise_invalid_query`, `()`, `"invalid_query"`), (`raise_query_timeout`, `()`, `"query_timeout"`), (`raise_upstream_unavailable`, `()`, `"upstream_unavailable"`). For `invalid_filter_op` and `invalid_cursor` (no canonical helper — raised inline in filter_compiler / tools/retrieval) directly construct `ToolError("invalid_filter_op: value required for this operator")` and `ToolError("invalid_cursor: shape mismatch")` and assert prefix — this is a contract assertion on the literal string form, not a regression test of those raise sites. (3) `test_error_includes_request_id` — exercise the ErrorEnrichmentMiddleware contract directly. Bind `bind_contextvars(request_id="rid-abc")`; build a minimal MiddlewareContext mock via `types.SimpleNamespace(message=types.SimpleNamespace(name="dummy"))`. Define `async def call_next(_ctx): raise ToolError("unknown_database: Database not found: foo")`. Await `ErrorEnrichmentMiddleware().on_call_tool(ctx, call_next)` inside `pytest.raises(ToolError) as exc_info`. Assert `exc_info.value.message == "unknown_database: Database not found: foo [request_id: rid-abc]"`. Cleanup with `clear_contextvars()` in a try/finally. (4) `test_upstream_4xx_no_echo` — exercise the principle that upstream Datasette message bodies are NEVER echoed in the ToolError message. Direct-assert on the two new helpers: `raise_query_timeout()` produces a literal-only message (no `{...}` substitution possible — verified via source inspection). `raise_upstream_unavailable()` produces a literal-only message. Build the assertion as: `with pytest.raises(ToolError) as exc_info: raise_upstream_unavailable()`; assert `exc_info.value.message == "upstream_unavailable: upstream call failed"` (exact equality — proves no upstream body bleed). Also verify that the canonical UpstreamCallFailed-to-ToolError mapping pattern in `tools/retrieval.py` (already implemented in Phase 3) does not echo upstream bodies: import `UpstreamCallFailed` from `core.datasette_client`, construct `UpstreamCallFailed("upstream 400 on /search.json: <evil user query body>", status=400)`, and assert that converting it to the canonical ToolError via `raise_upstream_unavailable()` discards the constructor message entirely (the helper does not consume the exception, the message is FIXED). Document this as the test gate that prevents future regressions from echoing upstream body text. Note: do NOT modify conftest.py — single-plan-touch rule (07-01 only).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_error_catalog.py::test_all_11_codes_in_catalog -x` exits 0.
    - `uv run pytest tests/test_error_catalog.py::test_all_errors_have_stable_code -x` exits 0.
    - `uv run pytest tests/test_error_catalog.py::test_error_includes_request_id -x` exits 0.
    - `uv run pytest tests/test_error_catalog.py::test_upstream_4xx_no_echo -x` exits 0.
    - `git diff tests/conftest.py` is empty (single-plan-touch preserved).
    - `uv run pytest -x` exits 0 (full suite GREEN).
    - `grep -c 'def test_' tests/test_error_catalog.py` outputs `4`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_error_catalog.py -x</automated>
  </verify>
  <done>ERR-01/02/03/05 contracts proven in four GREEN tests; CATALOG is a single source of truth; ErrorEnrichmentMiddleware appends request_id; upstream-4xx-no-echo principle is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream Datasette body → ToolError message | Datasette 4xx bodies may contain user-supplied query text — must never be echoed |
| FastMCP `on_call_tool` exception path → client | Error envelope reaches the client verbatim; request_id correlation must be present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering / Repudiation | Error code drift (renaming a code breaks MCP clients) | mitigate | `CATALOG` constant in `core/errors.py` is the single source of truth; `test_all_11_codes_in_catalog` mechanically asserts the locked set + order. Any rename requires editing both the constant and the test in one commit, surfacing the contract change in code review. |
| T-07-08 | Information Disclosure | Upstream body echo into ToolError | mitigate | `raise_upstream_unavailable()` produces a FIXED literal — never consumes the constructor message; `test_upstream_4xx_no_echo` proves the helper discards the upstream body. INJ-05 carry-forward from Phase 3 preserved. |
</threat_model>

<verification>
- `uv run pytest tests/test_error_catalog.py -x` is GREEN.
- `uv run pytest -x` is GREEN.
- `python -c "from mcp_zeeker.core.errors import CATALOG; assert len(CATALOG) == 11"` exits 0.
- `grep -c 'CATALOG' src/mcp_zeeker/core/errors.py` is `>= 1`.
- `grep -c 'ErrorEnrichmentMiddleware' src/mcp_zeeker/server.py` is `>= 2`.
</verification>

<success_criteria>
- `core/errors.py` defines the 11-code CATALOG tuple in REQUIREMENTS.md order.
- Two new raise helpers (`raise_query_timeout`, `raise_upstream_unavailable`) emit FIXED literal messages.
- `ErrorEnrichmentMiddleware` registered in FastMCP middleware chain AFTER RetrievedAtMiddleware.
- Four GREEN tests cover ERR-01, ERR-02, ERR-03, ERR-05.
- No regression on existing tool tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-04-SUMMARY.md` documenting: the CATALOG tuple contents (in order), the two new raise helpers and their fixed-literal messages, the FastMCP middleware chain order after wiring (RequestId at ASGI → RateLimit at ASGI → Origin at ASGI → FastMCP RetrievedAt → FastMCP ErrorEnrichment → handler), and the four GREEN tests.
</output>
