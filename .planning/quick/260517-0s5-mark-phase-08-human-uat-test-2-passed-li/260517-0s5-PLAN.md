---
phase: quick-260517-0s5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DOC-UAT-08-2
  - DOC-STATE-REFRESH

must_haves:
  truths:
    - "08-HUMAN-UAT.md test #2 result is `passed` with the 2026-05-17 evidence block listing 11 live test names"
    - "08-HUMAN-UAT.md test #1 remains `pending` with a note describing the SOAK_BYPASS_TOKEN mismatch blocker (2026-05-16 12:59 UTC, run 25962546108, HTTP 404 from /admin/metrics)"
    - "08-HUMAN-UAT.md Summary counters reflect passed: 1, pending: 1, total: 2"
    - "08-HUMAN-UAT.md frontmatter `updated:` is 2026-05-17"
    - "STATE.md frontmatter `status:` is no longer `ready_to_plan`; it accurately reflects phase-8-only blocker (HUMAN-UAT #1 / soak token rotation)"
    - "STATE.md frontmatter `stopped_at:`, `last_updated:`, `last_activity:` reflect 2026-05-17 reality (HUMAN-UAT #2 passed 11/11 locally; only soak gate outstanding)"
    - "STATE.md Resume narrative no longer points at the stale `/gsd-plan-phase 7 --gaps` recommendation"
  artifacts:
    - path: .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md
      provides: "Refreshed HUMAN-UAT ledger with test #2 marked passed and evidence"
      contains: "result: passed"
    - path: .planning/STATE.md
      provides: "Refreshed project state reflecting phase 8 / HUMAN-UAT #1 as the only open gate"
      contains: "2026-05-17"
  key_links:
    - from: .planning/STATE.md
      to: .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md
      via: "Resume narrative pointing operator at the remaining soak gate"
      pattern: "08-HUMAN-UAT"
---

<objective>
Refresh two planning ledgers to reflect 2026-05-17 reality: HUMAN-UAT test #2 (live integration tests vs data.zeeker.sg) passed locally 11/11 today, and HUMAN-UAT test #1 (24h soak) is still pending — currently blocked on a SOAK_BYPASS_TOKEN mismatch between the GitHub Actions secret and the production container's env (last attempted run 2026-05-16 12:59 UTC, GitHub Actions run id 25962546108, failed at preflight with HTTP 404 from `/admin/metrics`).

Purpose: STATE.md currently says `status: ready_to_plan` and the Resume narrative points at a stale phase-7 gap closure that no longer applies. The HUMAN-UAT ledger still shows both tests as `[pending]` even though the live tests are green. Both files mislead future sessions; this is a pure docs sync.

Output: An updated `08-HUMAN-UAT.md` (test #2 → passed with evidence, summary counters bumped, `updated:` timestamp = 2026-05-17, test #1 left pending but with a fresh blocker note) and an updated `STATE.md` (status / stopped_at / last_updated / last_activity / Resume narrative all consistent with: phase 8's only remaining gate is HUMAN-UAT #1, blocked on operator-side soak-token rotation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md

<background>
**HUMAN-UAT #2 evidence — 11 live tests passed locally on 2026-05-17 via `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest -m live -v`:**

- tests/test_heavy_column_upstream.py::test_mlaw_news_heavy_column_returns_content
- tests/test_live_golden_path.py::test_live_list_databases
- tests/test_live_golden_path.py::test_live_list_tables
- tests/test_live_golden_path.py::test_live_describe_table
- tests/test_live_golden_path.py::test_live_search
- tests/test_live_golden_path.py::test_live_query_table
- tests/test_live_golden_path.py::test_live_fetch
- tests/test_metadata_cache.py::test_live_metadata_parseable[zeeker-judgements]
- tests/test_metadata_cache.py::test_live_metadata_parseable[pdpc]
- tests/test_metadata_cache.py::test_live_metadata_parseable[sg-gov-newsrooms]
- tests/test_metadata_cache.py::test_live_metadata_parseable[sglawwatch]

Note: the existing test #2 `expected:` line names only 6 live tests; the actual `-m live` selector now also picks up the `test_metadata_cache.py::test_live_metadata_parseable[*]` parametrisations and the heavy-column upstream test. Preserve the original `expected:` wording (it is not wrong — those 6 still pass) and add the broader 11-test list in the evidence block under `result:`.

**HUMAN-UAT #1 current blocker (for the new ops_note addendum):**

- Last attempted dispatch: 2026-05-16 12:59 UTC, GitHub Actions run id 25962546108
- Failure: preflight step against `/admin/metrics` returned HTTP 404
- Root cause: `SOAK_BYPASS_TOKEN` value held by the GitHub Actions secret does not match the value baked into the prod container's env → bypass middleware does not authorise the request → metrics route returns 404 (route is conditional on bypass).
- Unblock action: operator must regenerate the token (or copy the existing GH secret value), set it in the prod container's env_file, restart the container, re-trigger `soak.yml`.

**STATE.md drift to repair:**

- `status: ready_to_plan` — WRONG. All 38 plans across phases 1-10 are completed (`completed_plans: 38`, `percent: 100`). The only outstanding gate is operator-side soak execution. Use `status: phase_8_pending_uat_1` (or similarly accurate phrasing — preserve the field's enum-like flavour from prior STATE entries).
- `stopped_at` references phase 7 / CR-01 / CR-02 — WRONG. Both CRs are resolved (HUMAN-UAT file's Gaps section already confirms it). Should point at HUMAN-UAT #1 awaiting operator token rotation.
- `last_updated` / `last_activity` are 2026-05-15 — stale. Set to 2026-05-17 with a one-line note that HUMAN-UAT #2 passed locally 11/11.
- The "**Resume:**" paragraph in the body recommends `/gsd-plan-phase 7 --gaps` — STALE. Replace with operator-facing guidance: phase 8's only blocker is rotating SOAK_BYPASS_TOKEN on prod, then triggering `soak.yml`, then running `/gsd-verify-work 8`.
- Leave the "Session Continuity" block's history values alone unless they create a contradiction — those record a past session. Add a fresh session line for 2026-05-17 if the block's pattern allows; otherwise update its `Last session` / `Stopped at` / `Resume file` to today's reality.
</background>

<interfaces>
**File 1 — `.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md` (existing schema, preserve exactly):**

```
---
status: partial
phase: 08-full-tests-24h-soak
source: [08-VERIFICATION.md]
started: 2026-05-15T06:58:12Z
updated: 2026-05-15T07:30:00Z      # → bump to 2026-05-17T<HH:MM>Z (use current time, UTC)
---

## Current Test
[awaiting human testing]

## Tests

### 1. Run the full 24h soak …
expected: …
result: [pending]                  # → KEEP as pending
prerequisites:
- …
- …
- …
ops_note: |
  …                                # → APPEND a new line describing the 2026-05-16 12:59 UTC failure (run 25962546108, /admin/metrics 404, SOAK_BYPASS_TOKEN mismatch). Do NOT rewrite the existing wording — append.

### 2. Run live integration tests against data.zeeker.sg
expected: …                        # → KEEP as-is
result: [pending]                  # → CHANGE to: passed
command: `ZEEKER_LIVE=1 uv run pytest -m live -v`
prerequisites: (none — CR-01 was fixed in commit 6654c71)
# → ADD an `evidence:` block immediately under `prerequisites:` listing date (2026-05-17), command used (`UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest -m live -v`), and the 11 test names from <background>. Use the same key: value / list shape the rest of the file uses (plain YAML-flavoured prose, no fenced code blocks — the file is markdown but each test stanza reads like YAML).

## Summary
total: 2
passed: 0          # → 1
issues: 0
pending: 2         # → 1
skipped: 0
blocked: 0

## Gaps
… (no changes needed — leave the existing operational-not-defect framing intact)
```

**File 2 — `.planning/STATE.md` (existing frontmatter shape; preserve all unchanged fields):**

```
---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: ready_to_plan          # → phase_8_pending_uat_1
stopped_at: "Phase 8 verification — human_needed (4/5 SCs; CR-01 + CR-02 confirmed unfixed in 08-REVIEW.md)"
                               # → "Phase 8 HUMAN-UAT #1 (24h soak) — pending operator-side SOAK_BYPASS_TOKEN rotation on prod container; HUMAN-UAT #2 (live tests) passed 11/11 locally 2026-05-17"
last_updated: "2026-05-15T06:58:59.906Z"   # → "2026-05-17T<HH:MM:SS.sssZ>" using current UTC
last_activity: 2026-05-15 -- Phase 08 verification = human_needed; HUMAN-UAT persisted
                               # → 2026-05-17 -- HUMAN-UAT #2 passed 11/11 locally; HUMAN-UAT #1 awaiting SOAK_BYPASS_TOKEN rotation
progress: (unchanged — totals stay the same; this is a docs sync, not a plan completion)
---
```

Body changes to STATE.md:
- "## Current Position" → flip `Status: Ready to plan` to match the new frontmatter status; update `Last activity:` to 2026-05-17.
- "**Resume:**" paragraph → fully replace. New content (paraphrase, do not quote verbatim): "Phase 8 has 1 remaining gate — HUMAN-UAT #1 (24h soak). It is blocked on operator-side token rotation: the `SOAK_BYPASS_TOKEN` value in the GitHub Actions repo secret does not match the value in the production container's env (last attempted dispatch 2026-05-16 12:59 UTC, run 25962546108, preflight failed with HTTP 404 on `/admin/metrics`). Unblock: regenerate or re-sync the token, redeploy/restart the prod container, re-trigger `soak.yml` via workflow_dispatch, then `/gsd-verify-work 8`. HUMAN-UAT #2 (live tests vs data.zeeker.sg) passed 11/11 locally on 2026-05-17 — evidence in 08-HUMAN-UAT.md."
- "## Session Continuity" → update `Last session:` to a 2026-05-17 ISO timestamp, `Stopped at:` to mirror the new `stopped_at`, and `Resume file:` to `.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md`.
- Leave the "Decisions", "Pending Todos", "Blockers/Concerns", "Deferred Items", "Performance Metrics" sections untouched — none of this work changes them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh 08-HUMAN-UAT.md — mark test #2 passed with evidence, add soak blocker note</name>
  <files>.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md</files>
  <action>
    Edit `.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md` in place, preserving the existing YAML-frontmatter + "## Current Test" + "## Tests" + "## Summary" + "## Gaps" structure exactly:

    1. Frontmatter: change `updated: 2026-05-15T07:30:00Z` → `updated: 2026-05-17T<HH:MM:SS>Z` using the current UTC time at execution. Leave `status: partial`, `phase:`, `source:`, `started:` unchanged.

    2. Test #1 (24h soak): keep `result: [pending]`. Keep the existing `prerequisites:` list and the existing `ops_note: |` block content. APPEND a new paragraph inside the existing `ops_note: |` block (continuation lines, indented like the existing ones) noting: most recent attempted dispatch 2026-05-16 12:59 UTC (GitHub Actions run id 25962546108) failed at the preflight step with HTTP 404 from `/admin/metrics` — root cause is a SOAK_BYPASS_TOKEN mismatch between the GitHub Actions secret and the production container's env; unblock by re-syncing the token in both places and restarting the prod container.

    3. Test #2 (live integration tests): change `result: [pending]` → `result: passed`. Leave `expected:`, `command:`, and `prerequisites:` lines unchanged. Immediately after the `prerequisites:` line, insert an `evidence:` stanza listing:
       - date: 2026-05-17
       - command: `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest -m live -v`
       - tests: the full 11-test list from the plan's `<background>` (one test per line, indented as a YAML-style list using `-`).

    4. Summary block: change `passed: 0` → `passed: 1` and `pending: 2` → `pending: 1`. Leave `total: 2`, `issues: 0`, `skipped: 0`, `blocked: 0` unchanged.

    5. Leave the "## Gaps" section completely untouched.

    Do NOT introduce fenced code blocks — the file's existing stanzas use plain `key: value` lines and `- item` lists; match that style. Do NOT reorder existing keys.
  </action>
  <verify>
    <automated>grep -E "^result: passed$" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md | wc -l | grep -q "^[[:space:]]*1$" &amp;&amp; grep -E "^result: \[pending\]$" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md | wc -l | grep -q "^[[:space:]]*1$" &amp;&amp; grep -q "^passed: 1$" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md &amp;&amp; grep -q "^pending: 1$" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md &amp;&amp; grep -q "2026-05-17" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md &amp;&amp; grep -q "test_live_list_databases" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md &amp;&amp; grep -q "test_live_metadata_parseable\[zeeker-judgements\]" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md &amp;&amp; grep -q "25962546108" .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md</automated>
  </verify>
  <done>
    File has exactly one `result: passed` (test #2) and exactly one `result: [pending]` (test #1); Summary shows `passed: 1` and `pending: 1`; an evidence block lists 2026-05-17 plus all 11 live test names; the soak blocker note mentions GitHub Actions run id 25962546108; frontmatter `updated:` is 2026-05-17.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh STATE.md — status, stopped_at, timestamps, Resume narrative</name>
  <files>.planning/STATE.md</files>
  <action>
    Edit `.planning/STATE.md` in place, preserving the existing frontmatter key order and all body sections except those listed below:

    1. Frontmatter:
       - `status: ready_to_plan` → `status: phase_8_pending_uat_1`
       - `stopped_at:` → `"Phase 8 HUMAN-UAT #1 (24h soak) — pending operator-side SOAK_BYPASS_TOKEN rotation on prod container; HUMAN-UAT #2 (live tests) passed 11/11 locally 2026-05-17"`
       - `last_updated:` → `"2026-05-17T<HH:MM:SS.sssZ>"` using current UTC (millisecond ISO 8601, matching the existing format)
       - `last_activity:` → `2026-05-17 -- HUMAN-UAT #2 passed 11/11 locally; HUMAN-UAT #1 awaiting SOAK_BYPASS_TOKEN rotation`
       - Leave `gsd_state_version`, `milestone`, `milestone_name`, and the entire `progress:` block unchanged.

    2. Body — "## Current Position":
       - Change `Status: Ready to plan` → `Status: Phase 8 awaiting HUMAN-UAT #1 (24h soak)`.
       - Change `Last activity: 2026-05-15` → `Last activity: 2026-05-17`.
       - Leave the `Phase: 9` and `Plan: Not started` lines as-is (the milestone roll-up is unchanged; this is operator-gate territory, not new planning).

    3. Body — Replace the existing `**Resume:**` paragraph (the one currently recommending `/gsd-plan-phase 7 --gaps`) with a new paragraph saying, in your own words: Phase 8's only remaining gate is HUMAN-UAT #1 (24h soak); it is blocked on operator-side rotation of `SOAK_BYPASS_TOKEN` (the GitHub Actions secret value does not match the prod container env, causing the last dispatch on 2026-05-16 12:59 UTC, run id 25962546108, to fail preflight with HTTP 404 on `/admin/metrics`); unblock by re-syncing the token in both places, restarting the prod container, re-triggering `soak.yml`, then running `/gsd-verify-work 8`; HUMAN-UAT #2 (live tests) passed 11/11 locally on 2026-05-17 — evidence in `.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md`.

    4. Body — "## Session Continuity":
       - `Last session:` → `2026-05-17T<HH:MM:SS.sssZ>` using current UTC.
       - `Stopped at:` → mirror the new frontmatter `stopped_at:` value (without the surrounding quotes).
       - `Resume file:` → `.planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md`.

    5. Leave "## Project Reference", "## Performance Metrics", "## Accumulated Context" (Decisions / Pending Todos / Blockers/Concerns), and "## Deferred Items" completely untouched.

    6. Do NOT touch the progress bar line (`Progress: [██████████████░░░░░░] 24/25 plans (96%) …`); it tracks milestone plans, not UAT gates.
  </action>
  <verify>
    <automated>grep -q "^status: phase_8_pending_uat_1$" .planning/STATE.md &amp;&amp; ! grep -q "^status: ready_to_plan$" .planning/STATE.md &amp;&amp; grep -q "2026-05-17" .planning/STATE.md &amp;&amp; ! grep -q "/gsd-plan-phase 7 --gaps" .planning/STATE.md &amp;&amp; grep -q "SOAK_BYPASS_TOKEN" .planning/STATE.md &amp;&amp; grep -q "HUMAN-UAT" .planning/STATE.md &amp;&amp; grep -q "25962546108" .planning/STATE.md &amp;&amp; grep -q "08-HUMAN-UAT.md" .planning/STATE.md</automated>
  </verify>
  <done>
    Frontmatter `status` is `phase_8_pending_uat_1`; `stopped_at`, `last_updated`, `last_activity` all reflect 2026-05-17 reality; the stale `/gsd-plan-phase 7 --gaps` recommendation is gone; the new Resume paragraph names the SOAK_BYPASS_TOKEN blocker and references the HUMAN-UAT.md file; Session Continuity's `Resume file:` points at `08-HUMAN-UAT.md`.
  </done>
</task>

</tasks>

<verification>
1. Both edited files parse as valid markdown (no broken frontmatter delimiters):
   - `head -1 .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md` is `---` and a matching closing `---` exists before the body.
   - `head -1 .planning/STATE.md` is `---` and a matching closing `---` exists before the body.
2. No remaining occurrence of `result: [pending]` for test #2's stanza specifically — the only surviving `[pending]` is on test #1's `result:` line.
3. No remaining reference to "phase 7" gap closure or CR-01 / CR-02 in the STATE.md Resume narrative (those are resolved per the HUMAN-UAT.md Gaps section).
4. Cross-link sanity: `.planning/STATE.md`'s Session Continuity `Resume file:` resolves to a real file:
   `test -f .planning/phases/08-full-tests-24h-soak/08-HUMAN-UAT.md`
</verification>

<success_criteria>
- `08-HUMAN-UAT.md` Summary block reads `total: 2`, `passed: 1`, `pending: 1`, `issues: 0`, `skipped: 0`, `blocked: 0`.
- `08-HUMAN-UAT.md` test #2 carries an evidence block with date 2026-05-17 and all 11 live test names from `<background>`.
- `08-HUMAN-UAT.md` test #1's `ops_note:` includes a fresh paragraph naming the 2026-05-16 12:59 UTC failure, run id 25962546108, the HTTP 404 on `/admin/metrics`, and the SOAK_BYPASS_TOKEN-mismatch root cause.
- `STATE.md` frontmatter `status` is no longer `ready_to_plan`; its `stopped_at`, `last_updated`, and `last_activity` all reflect 2026-05-17.
- `STATE.md` body Resume narrative recommends operator-side SOAK_BYPASS_TOKEN rotation, not a phase-7 gap closure.
- Neither file's untouched sections (Gaps in HUMAN-UAT; Decisions / Pending Todos / Blockers / Deferred Items / Performance Metrics in STATE) shows any incidental edits.
</success_criteria>

<output>
After completion, create `.planning/quick/260517-0s5-mark-phase-08-human-uat-test-2-passed-li/260517-0s5-SUMMARY.md` describing:
- Which lines changed in each file (delta-style, 5-10 bullets max)
- The new Summary counters in `08-HUMAN-UAT.md`
- The new `status:` / `stopped_at:` values in `STATE.md`
- Any timestamps used (UTC ISO 8601)
</output>
